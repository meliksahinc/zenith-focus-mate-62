services:
  focus-coach:
    build: .
    ports:
      - "8080:5173"
    environment:
      - NODE_ENV=production
    # Remove webcam device mapping for macOS compatibility
    # Webcam access will be handled by browser permissions
    command: npm run preview -- --host 0.0.0.0 --port 5173
